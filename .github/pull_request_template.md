## What does this PR do?

<!-- One sentence summary. -->

## Related issue

Closes #<!-- issue number, if any -->

## Checklist

- [ ] Tests written or updated — all suites pass (`xcodebuild test -scheme VIGTests`)
- [ ] No new external dependencies introduced
- [ ] If touching `InputBlockingService` or `DisplayManagerService`: Cocoa ↔ CGEvent coordinate flip verified
- [ ] If adding a service: `*Protocol` interface + mock in `VIGTests/Mocks/` included
- [ ] Commit messages follow Conventional Commits (`feat:`, `fix:`, `refactor:`, etc.)

## Notes for reviewer

<!-- Anything non-obvious about the approach, trade-offs considered, or areas to focus on. -->
